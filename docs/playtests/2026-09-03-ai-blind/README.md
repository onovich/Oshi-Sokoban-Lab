# 2026-09-03 · 三代理玩家视角试玩

## 状态与证据边界

已完成 A、B、C 三位独立 AI 代理的实际浏览器试玩：每位完成同样的 19 关，共 57 次通关观察，覆盖 19 个不同关卡。本轮不属于真人盲测，不计入课程计划要求的真人样本数，不用于把目标难度改写为实测人类难度，也不替代作者验收。

先读 [反馈汇总](summary.md)。汇总前部不含解法，后部与三份原始记录包含解题过程，建议作者本人试玩后再看。

三个代理均以 `fork_turns: none` 启动，没有继承设计讨论、解法、源代码分析或目标难度。它们使用同一模型，因此也不能视为三个具有独立人类经验分布的样本。协调者知道设计背景，但不参与解题、不向测试者解释谜题、不在试玩期间改动关卡。

## 试玩环境

- 正常游戏：`http://127.0.0.1:5174/`。
- 本地玩家版入口：`http://127.0.0.1:5174/playtest.html`，复用同一 App 和规则引擎。
- 玩家版关闭作者工具、内部命题/证明分析和难度信息，自由选关。
- 每位代理创建独立标签页，分别使用 `?tester=A`、`?tester=B`、`?tester=C` 进行人工识别；该查询参数本身不改变规则。
- 玩家版使用标签页独立的 sessionStorage，不读取或写入正常游戏的 localStorage 进度。
- 源版本：`ba6998b39d46ce0f68fd5df1bb17714a4090929e` 加当前未提交的顺序导航修复与玩家版入口；关卡盘面不变。
- 关卡文件 SHA-256：`src/levels/mastery/push-footprint-levels.ts` = `1B8AC55F9325B7623ECAF8B834173A7D457F52E250BC53D47DB097703E2B00D3`。
- 规则引擎 SHA-256：`src/engine/game-engine.ts` = `59D4F8C080779F7DACAAE36BD25943248762F0A28BEFCB902A6C3F120F59DDDB`。
- 开始前机器验证：26 个测试文件、223 项测试通过；生产构建通过。玩家版入口仅供本地开发试玩，不修改生产入口。

## 给测试者的共同信息

只告知游戏入口、操作界面读取权限、试玩顺序和反馈格式。玩法规则全部通过玩家实际可见的目标、关卡说明、图形与操作结果了解。

1. 按显示编号 `01 → 02 → 03 → 05 → 06 → 07 → 09 → 10 → 11` 做短热身。
2. 依次试玩 `31 → 32 → 33 → 34 → 35 → 36`。
3. 仍有时间时依次试玩 `57 → 58 → 59 → 60`。
4. 约 25 分钟主动试玩为单人上限；可以如实记录卡住或未试玩，不强迫填满结果。

这些热身并不等于完成全部基础课程。若新关依赖尚未经历的规则，应将前置知识不足单独记录，不能全部归因于关卡推理难度。

禁止读取项目源码、测试、求解器、研究文档、目标难度、隐藏属性、组件状态、浏览器存储或网络模块；禁止攻略搜索、穷举程序与互相交流。棋盘主要通过截图观察，DOM 只用于读取正常可见文案及定位界面控件。采用各自标签页内的方向按钮，避免系统键盘焦点串扰。

## 记录方式

逐关记录第一假设、实际关键尝试、胜利证据、步数、撤销/重开、形成的认识、技巧迁移、认知难度/执行负担/趣味各 1–10 的代理主观评分，以及图形/动画/控制歧义。未精确统计的指标明确留空或标记未知。

壁钟时间含浏览器调用、截图和模型推理延迟，不能直接与人类完成时间比较。评分是代理报告，不是目标评级，不做三人平均后宣称“校准通过”的推断。

## 原始记录

- [测试者 A](tester-a.md)
- [测试者 B](tester-b.md)
- [测试者 C](tester-c.md)

## 实际信息暴露与限制

三位代理均没有继承此前设计对话，没有读取源码、求解器、内部命题或他人反馈。玩家版的正常关名和说明仍然可见；它们本身可能提示关键思路，因此不能把本轮称为“完全无提示的盘面测试”。

- A 在第 01 关定位方向按钮时，一次完整可访问快照连带显示了该关格子的可访问名称；随后不再读取棋盘 DOM，目标关都通过截图观察。
- B 与 C 的控件定位输出连带列出了课程地图按钮名称（B 明确记录包括折叠区项目）；没有读取这些关卡的盘面、解法或内部数据。
- 这些是原始记录中主动披露的边界偏差，不隐藏、不据此宣称绝对信息隔离。下一轮可预先提供正常按钮的可访问名，减少广域控件枚举。
- A 全流程未精确计时；B 约 20 分钟、C 约 23 分钟壁钟均包含工具与记录延迟。约 25 分钟只是本轮操作预算，不是人类解题时限。

三个代理都从可见的 0/70 开始，最终各自显示 19/70；正常玩家存档使用另一套持久存储。本轮没有修改关卡、规则、目标难度或验收状态。协调者在全部反馈收集期间独立复核了第 57 关的可见布局尺寸，该诊断不计作第四个试玩样本。
