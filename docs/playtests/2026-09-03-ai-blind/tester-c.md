# 试玩者 C：AI 浏览器盲测记录

日期：2026-09-03。入口：`http://127.0.0.1:5174/playtest.html?tester=C`。

这是一个 AI 代理通过正常玩家页面进行的独立试玩，不能当作真人盲测样本。未读取游戏源码、文档、隐藏状态或解法，也不与其他试玩者交换信息。通过截图观察棋盘，只读取可见控件文本定位操作。使用独立临时标签页和页面方向按钮。壁钟起点约 13:08 UTC；工具延迟不代表人类用时。Undo / Restart 计数为本轮实际调用数，若不确定会明确标记。

标签页 ID：`1`。初始界面进度：已完成 0/70，基础 0/94，主线 0/3，100% 0/120。

## 连续记录

### 01 — 推侧访问：开放的一侧

- 第一假设（行动前）：黄色是玩家，白色实心块可以向右推到四角框目标；预计连续右推即可。
- 初始歧义：没有格线，用物体宽度推断格距。
- 关键尝试：右、右。控件第一次按箭头字符定位失败，没有发出游戏动作；改按正常可访问名 `Move right` 定位成功。
- 结果：亲眼看到“已完成”，最终步数 2。Undo 0，Restart 0。
- 学到：黄色角色推动白块；碰到目标自动判定完成。前关迁移：无。
- 主观评分（认知难度 / 执行负担 / 趣味）：1 / 1 / 2。

### 02 — 推侧访问：先抵达箱后

- 第一假设（行动前）：先沿左侧向上，再从上侧绕到白块左边向右推动。
- 初始歧义：暗色区域与带边框的暗色区域是否分别为地面 / 墙，目前不完全确定；棋盘超出窗口，需滚动才能看全。
- 关键尝试 / 错误：先按上、上、右、下，只有右成功，步数显示 1；由此才确认上方大暗块是墙。随后上、右完成。最初把墙误读为可走区域。
- 结果：亲眼看到“已完成”，最终步数 3。Undo 0，Restart 0；另外有 3 次无效方向输入，不计入步数。
- 学到：应读白色轮廓边界而不是只看填色，需先抵达推侧。迁移了 01 的推动规则。
- 主观评分（认知难度 / 执行负担 / 趣味）：2 / 3 / 3。图形歧义比路径本身难。

### 03 — 推侧访问：两件物体的顺序

- 第一假设（行动前）：先推较低的白块向右到下方目标，再退回左侧向上去推另一块；左边通道应可走。
- 关键尝试：上、右×3，退左×3、上，再右×3；初始计划直接奏效，没有错误。
- 结果：亲眼看到“已完成”，最终步数 11。Undo 0，Restart 0。
- 学到：独立白块应分别从正确侧推动。迁移了 02 的绕行 / 墙轮廓理解。
- 主观评分（认知难度 / 执行负担 / 趣味）：2 / 2 / 3。
- 图形：相邻白块以细线区分，需仔细看；动画无明显歧义。

### 05 — 完整 footprint：一起移动

- 第一假设（行动前）：白色长块应该是一整件，推上半部会连带下半部一起移动；向右两次。
- 关键尝试：右×2，整体移动并到位。结果：亲眼看到“已完成”，最终步数 2，Undo 0，Restart 0。
- 学到：任一接触格都能推动整件长块；前关的两个相邻白块与本关外观太相似，主要靠标题预期和实际推动区别。
- 主观评分（认知难度 / 执行负担 / 趣味）：1 / 1 / 3。
- 图形：希望连体与分离白块有更明确的不同；fullPage 截图工具出现窄列异常，普通截图正常，该问题不归因于游戏逻辑。

### 06 — 完整 footprint：一格受阻

- 第一假设（行动前）：向右会被长块下半部前方墙挡住，应先从下方上推一格，再从左侧右推到目标。
- 关键尝试：先右（无效，符合预期），下×2、右、上，上移整块；左、上、右完成。
- 结果：亲眼看到“已完成”，最终步数 7，另有 1 次无效输入。Undo 0，Restart 0。
- 学到：要检查整个长块的目的区域；05 的整体推动规则直接用于规划。
- 主观评分（认知难度 / 执行负担 / 趣味）：2 / 3 / 4。
- 图形 / 控制：竖长棋盘与底部按钮无法同屏，点击方向按钮会自动滚动离开上部，观察负担明显。

### 07 — 完整 footprint：腾出的格子

- 第一假设（行动前）：L 形块先右移一次，腾出向上绕行的位置；从上方下推一次后，再从左侧右推。先连续右推会撞到右上墙。
- 关键尝试：右、上、右、下，再左、下、右；初始方案奏效，无错误。
- 结果：亲眼看到“已完成”，最终步数 7，Undo 0，Restart 0。
- 学到：移动本身会改变可用推侧；06 的完整目的区域检查迁移到 L 形。
- 主观评分（认知难度 / 执行负担 / 趣味）：3 / 3 / 5。
- 图形 / 控制：形状轮廓可读，但大棋盘仍要求上下滚动。

### 09 — 完整覆盖：两格都要到位

- 第一假设（行动前）：与 05 的竖长块盘面近似，右推两次应该完整覆盖目标。
- 关键尝试：右×2，无错误。结果：亲眼看到“已完成”，最终步数 2，Undo 0，Restart 0。
- 学到 / 迁移：直接复用 05，没有明显新增认知。
- 主观评分（认知难度 / 执行负担 / 趣味）：1 / 1 / 2。
- 图形 / 控制 / 动画：无新增歧义。

### 10 — 完整覆盖：局部重合不算

- 第一假设（行动前）：第一个框只是局部重合的位置；需要继续右推到最右竖长目标。预计两步，第一步不完成。
- 关键尝试：右一次看到“进行中”，再右一次看到“已完成”。没有错误。
- 结果：最终步数 2，Undo 0，Restart 0。
- 学到：部分白块接触框不会过关；可留有未被白块覆盖的其他框。直接使用 09 的完整覆盖认识。
- 主观评分（认知难度 / 执行负担 / 趣味）：1 / 1 / 3。
- 图形：框被白块 / 黄色角色遮住后不再明显，靠记忆保留目标位置。

### 11 — 完整覆盖：读完整目标图案

- 第一假设（行动前）：类似 07，但上方墙更宽，要先右推两格才能绕上去下推，再从左侧补一次右推。
- 关键尝试：右、右、上、右、下，再左、下、右；无错误，首次方案完成。
- 结果：亲眼看到“已完成”，最终步数 8，Undo 0，Restart 0。
- 学到 / 迁移：07 的绕上方再改变推进方向直接迁移；10 的完整覆盖约束始终用于目标判断。
- 主观评分（认知难度 / 执行负担 / 趣味）：3 / 2 / 5。
- 图形 / 控制 / 动画：没有新增歧义。

热身节点：01、02、03、05、06、07、09、10、11 共 9 关均看到胜利。

### 31 — 回身余地

- 首次进入目标关前已截图，UI 显示第 31 关、0 步、已完成 9/70。
- 第一假设（行动前）：看不到目标框，猜测其被初始角色 / 白块遮住；先右推一格应能进入下方小回路，再绕至右侧回推白块。
- 关键尝试：右一步后初始黄色位置露出目标框，确认猜测；下、右、右、上绕到右侧；左、左完成。无错误。
- 结果：亲眼看到“已完成”，最终步数 7，Undo 0，Restart 0。
- 学到：先朝远离目标的方向推也可能是在创造回身位置。07 / 11 的“腾出站位”认识用到了这里。
- 主观评分（认知难度 / 执行负担 / 趣味）：3 / 2 / 5。
- 图形 / 控制 / 动画：初始目标被黄色角色完全遮住，必须先动一步才看得到；这增加信息发现成本。动作反馈清晰。

### 32 — 门留下的格子

- 第一假设（行动前）：先把竖长块向右推到近处目标，空出的竖通道可以向上走，再右推上方单块到远端目标。
- 关键尝试：右、上、上，再右、右。首次方案奏效，无错误。
- 结果：亲眼看到“已完成”，最终步数 5，Undo 0，Restart 0。
- 学到 / 迁移：长块占据的旧格子可用作通路，直接使用 07 / 11 / 31 的认识；本关没有让我停下来重新考虑的冲突。
- 主观评分（认知难度 / 执行负担 / 趣味）：2 / 1 / 4。
- 图形 / 控制 / 动画：没有新增歧义。

### 33 — 离开终点

- 第一假设（行动前）：长块下方看不到目标，结合标题猜测它已经在目标上；先像 32 一样右推长块让出通道，完成上方单块后，利用右上多出的一格把长块推回。
- 关键尝试：右、上、上后看到长块原位的两格目标；右、右、下、左完成。无错误。
- 结果：亲眼看到“已完成”，最终步数 7，Undo 0，Restart 0。
- 学到 / 迁移：目标上的块仍可临时移动；32 的腾出通道加上 31 的回推形成完整方案。标题在行动前帮助判断了隐藏目标。
- 主观评分（认知难度 / 执行负担 / 趣味）：3 / 2 / 5。
- 图形 / 控制 / 动画：已停靠长块与普通长块外观相同，初始目标不可见；没有靠视觉单独确认其已到位。

### 34 — 共用的绕行线

- 第一假设（行动前）：左长块应该也盖着目标；先右移它让出左侧竖通道，从上方走到右长块左边，把右长块推到最右目标，最后回推左长块。
- 关键尝试：右、上、上、右、右，再下、右、左、左。首次方案奏效，无错误。
- 结果：亲眼看到“已完成”，最终步数 9，Undo 0，Restart 0。
- 学到 / 迁移：33 的临时移开后复位几乎原样迁移；多一个右侧长块，但没有增加需要试错的分支。
- 主观评分（认知难度 / 执行负担 / 趣味）：3 / 2 / 4。
- 图形 / 控制 / 动画：初始已到位的目标依旧被遮住；没有新增控制问题。

### 35 — 临时支点

- 第一假设（行动前）：盘面出现雨线，尚不知道是否改变移动；先按一次右观察移动距离。两组白色区域看似需要分别移向右侧和下方目标，先从顶部接近。
- 关键尝试：一次右滑过多格，停在白块前；下一次下把下方长块只推一格到位。原本考虑再上移回顶部，但观察后发现当前已经贴着另一长块下半部，可直接右、右完成。没有错误 / 撤销。
- 结果：亲眼看到“已完成”，最终步数 4，Undo 0，Restart 0。
- 学到：雨中普通移动会滑到阻挡前，而这几次推块各前进一格；可以从长块任一相邻格接触推动。未充分实验雨中其他邻接规则，不能声称已经掌握。
- 前关迁移：05 / 06 的整体推动；没有使用外部或未试玩课程的雨规则。
- 主观评分（认知难度 / 执行负担 / 趣味）：3 / 1 / 5。
- 图形 / 控制 / 动画：雨线使新环境有明确视觉提示，但页面操作说明并未解释移动距离变化；步数按输入计，不能与普通关步数直接比较。

### 36 — 远处先完成

- 第一假设（行动前）：先完成左边近目标会堵住唯一通向右边的窄口；应从左块上方绕行到右块，完成远端，再原路回来推近块。标题直接提示了顺序，降低了自行发现的难度。
- 关键尝试：上、上、右、右、下，到达中央；右、右完成右块；左、左、上、左、左、下、右回来完成左块。无错误。
- 结果：亲眼看到“已完成”，最终步数 14，Undo 0，Restart 0。
- 学到 / 迁移：不能只看下一次能否到目标，要看该位置会不会阻断另一个推侧；前关的共享通道和顺序规划用于此关。
- 主观评分（认知难度 / 执行负担 / 趣味）：4 / 3 / 5。
- 图形 / 控制 / 动画：未出现新歧义；这是目标 31–36 中动作最长的一关，但路径绕行仍较直接。

目标组节点：31–36 全部看到胜利，无 Undo / Restart。当前累计 15 关完成，继续按约定顺序尝试 57–60。

### 57 — 把地图转过来

- 第一假设（行动前）：与 36 的关系相像，但换成向下推进；先把上方长块推到近目标会挡住窄通道，所以从右侧绕下去完成远处长块，再回到上方推近块。
- 关键尝试：右、右、下、下、左，再下、下完成远块；上、上、右、上、上、左、下完成近块。首次方案奏效，无错误。
- 结果：亲眼看到“已完成”，最终步数 14，Undo 0，Restart 0。
- 学到 / 迁移：将 36 的通道占用关系按视觉旋转后直接应用，未产生新的机制猜测。
- 主观评分（认知难度 / 执行负担 / 趣味）：3 / 5 / 4。
- 图形 / 控制 / 动画：竖长棋盘是最明显负担，角色、远目标、控制不能同屏，必须多次滚动记忆；滚动后立即截图有黑块重绘现象，下一次静止截图恢复，无法归因于游戏本身。

### 58 — 三道门

- 第一假设（行动前）：三件长块依次向右到位；第一件移开后从上方绕到第二件，第二件移开后从下方绕到第三件。虽然 36 教过先远后近，这里上下回路让逐件完成似乎可行。
- 关键尝试：右、上、上、右、右；下、右、下、下；右、右、上、右。按每段新位置确认后继续，初始方案奏效，无错误。
- 结果：亲眼看到“已完成”，最终步数 13，Undo 0，Restart 0。
- 学到 / 迁移：是否要保留通路取决于实际绕行空间，不是所有近目标都该最后完成；32 的腾出格子 + 36 的检查后续访问共同使用。
- 主观评分（认知难度 / 执行负担 / 趣味）：4 / 2 / 5。
- 图形 / 控制 / 动画：横向盘面能一次看全，比 57 更轻松；无新增歧义。

### 59 — 差一格的近路

- 第一假设（行动前）：左边似乎是顶端向左伸出的 L 形块，目标的一格可能藏在它下面；若先右推到位会挡住去右块的通路。先沿上方绕到右端完成远块，再回来右推 L 形块。
- 关键尝试：上、右、右、右、下；右、右完成远块；左、左、上、左、左、左、下、右完成 L 形块。无错误，首次方案奏效。
- 结果：亲眼看到“已完成”，最终步数 15，Undo 0，Restart 0。
- 学到 / 迁移：L 形比竖长块多占一格，但沿用 36 / 57 的先远后近即可；没有经历“走近路失败”的发现过程。
- 主观评分（认知难度 / 执行负担 / 趣味）：4 / 3 / 4。
- 图形 / 控制 / 动画：目标部分被 L 形遮住；最初只能从轮廓推测连体关系，首次推动后才确认整件一起移动。

### 60 — 依次打开

- 第一假设（行动前）：初始相邻白色区域的分件关系不够明确，可能是水平长块与上方 / 右方单块相接；先向右推一次确认哪一部分一起动，再检查能否从腾出的通路接近上方白块。暂时没有完整解法。
- 实时尝试：右后水平白色区域移动；下成功，确认底部暗色区域是地面；右、上中的上无效，当前有效步数 3。仍未确定是否把水平长块停错位置。
- 后续关键尝试 / 错误：再右、右、上，把右侧单块推到上方目标；继续上被挡住，步数仍为 6。从右向左回推水平长块一步（7 步）后，右侧单块原位露出另一个目标格，才明确水平长块最终应停在更右的位置。该回推妨碍返回推侧，于是 Undo 1 次回到 6 步；下、左、左、左、上回到左侧，再右、上完成全部。
- 结果：亲眼看到“已完成”，最终步数 13；Undo 1，Restart 0；有 2 次无效上输入。没有重开本关。
- 学到：移动右侧单块既让出水平长块目的位置，也暴露了被挡住的目标；水平长块进一步向右后，才可以站到上方单块下面。这个先后关系需要根据实际完整目标重新判断。
- 前关迁移：检查推侧、完整形状、临时位置均有用，但先前对“近处目标在哪里”的猜测在本关误导了我；这是本次真正需要修正空间解释的一关。
- 主观评分（认知难度 / 执行负担 / 趣味）：7 / 4 / 6。
- 图形 / 控制 / 动画：目标被单块完全遮挡是主要卡点；起初还误读底部暗色区域为不可走。细分隔线不足以立即看清水平块和相邻单块的归属。Undo 按钮成功回退一次，反馈可理解。

## 结束记录与信息边界

- 实际顺序：01、02、03、05、06、07、09、10、11、31、32、33、34、35、36、57、58、59、60。19 关均实际看到“已完成”，没有跳过的卡住关；总 Undo 1，Restart 0。
- 最终界面核对：自建 tab `1`，第 60 关步数 13、已完成；顶部显示已完成 19/70，基础结业 17/94，主线结局 0/3，100% 19/120。初始对应值为 0/70、0/94、0/3、0/120。已截取最终普通窗口截图核对，未读取存储。
- 主动试玩结束壁钟约 13:31 UTC；从 13:08 UTC 建立浏览器至结束约 23 分钟，包含工具、截图、滚动与记录延迟，不是人类通关用时。
- 19 次初始假设均在对应首次动作前记录；第 33 条曾因补丁定位插错位置，结束时只移动整段到实际顺序位置，未改写首次假设。
- 信息来源自检：初次进入时读取了正常关卡选择器的完整选项名，以及可见胜利目标 / 操作说明。第 01 关按钮定位失败后，读取一次可见 DOM 获取方向按钮可访问名；该输出也列出了课程按钮名称，未据此查看后关盘面或解法。没有读取棋盘可访问名称、棋盘内部坐标、隐藏属性、组件状态、存储、网络、源码、项目文档、求解器或他人反馈。所有实际棋盘观察来自截图。
- 我做过少量明确、人工推理出的方向数组循环（最多 5 步一组），每组后观察截图；没有搜索、自动规划、穷举或求解程序。所有方向输入用页面按钮，仅操作自建 tab `1`。
- 这些分数是一个 AI 代理对自己这次操作的主观记录，不推断真人难度。31–36 大多第一次方案直接成功；标题与相邻关的结构相似帮助明显。第 60 关最需要重新理解，但耗费主要来自看不到完整目标与图形归属。
