# Oshi 课程盲测协议

自动求解器证明“存在解”；本协议检验玩家能否从盘面理解那条解为什么成立。未填写的关卡只能标为候选关。

## 每次盲测

- 测试者此前未见本关地图、内部命题或最短解。
- 只允许看到游戏正常提供的胜利目标、操作和已学过的图形语言。
- 观察者不提示；若测试者主动询问图标含义，只记录问题，不回答到本关结束。
- 因图标、遮挡、动画、输入或焦点歧义造成的失败记为关卡缺陷，不记为合理难度。

记录以下信息：

| 字段 | 说明 |
| --- | --- |
| 关卡 / 版本 | `lesson-xx` 与 Git commit |
| 测试者编号 | 匿名编号，不记录不必要的个人信息 |
| 已完成前置组 | 排除知识顺序不一致 |
| 首个口述假设 | 玩家最初认为规则或解法是什么 |
| 尝试路径 | 关键移动、回退和重开；不要求逐键转录 |
| 首次关键事件 | 何时触发 `criticalEvent` |
| 顿悟表述 | 玩家用自己的话解释新理解 |
| 重开 / Undo 数 | 区分推理成本和执行成本 |
| 顿悟后有效推动 | 从关键事件/口述顿悟到胜利的推动数 |
| 歧义 | 图标、盘面遮挡、动画或控制是否误导 |
| 结果 | 独立解出 / 提示后解出 / 放弃 |

## 通过标准

- 建立关：至少 3 次盲测；玩家通过操作形成目标假设，不靠说明文字获得答案。
- 定界关：至少 3 次盲测；地图本身能纠正建立关后最常见的过度概括。
- 推演关：至少 5 次盲测；至少 3/5 测试者无提示发现预定二阶后果。
- 按目标难度计，D1–3 每关至少 3 名目标玩家，D4–7 至少 5 名，D8–10 至少 8 名熟悉逻辑解谜的玩家；取角色标准与难度标准中较高的样本数。
- D10 至少要有 3/8 玩家跨会话独立解出，并能用自己的话清楚解释核心洞察。
- 任一关出现静态不可读关键状态，直接退回候选池。
- 推演关顿悟后超过 6 次有效推动，优先压缩执行尾巴。

## 记录模板

```md
### lesson-__ · commit ________ · tester-__

- 已完成前置组：
- 首个口述假设：
- 关键尝试路径：
- 首次关键事件：
- 顿悟表述：
- Restart / Undo：
- 顿悟后有效推动：
- 图标 / 遮挡 / 动画 / 控制歧义：无 / 有（说明）
- 结果：独立解出 / 提示后解出 / 放弃
- 观察者备注：
```

## 当前状态

`accepted-foundation-v1` 的 60 个冻结关已经完成作者游玩验收。3 个 Spike 销毁／重生原型已迁为 `lab-spike-*`，尚未完成作者验收，也不计正式进度。`mastery-v2` 当前有 70 个可玩正式关：60 个冻结关与 P1–P10；后十关已通过可解性、证明条件必要性、宏策略与元素变异机器审计，但尚未进行陌生玩家难度校准。因此 README 与课程文档不得把 D3–D8 目标标签描述成实测难度，也不得把作者验收描述成“全部完成人类可理解性验收”。

## P1–P10 样板弧校准

测试时必须按 P1 → P10 顺序进行，避免高阶技巧倒灌到前关。每关除通用记录外，还要记录玩家能否把前关技巧迁移到新形状／方向，以及是否出现与核心命题无关的机械执行。

| 关卡 | 目标难度 | 最少样本 | 本轮要验证的核心问题 |
| --- | ---: | ---: | --- |
| P1 `回身余地` | D3 | 3 | 玩家是否主动接受“第一推远离目标”以保留推侧。 |
| P2 `门留下的格子` | D3 | 3 | 玩家是否把多格物腾出的远端格读成唯一通路。 |
| P3 `离开终点` | D4 | 5 | 玩家是否愿意暂时破坏已完成状态来换取站位。 |
| P4 `共用的绕行线` | D4 | 5 | 玩家是否识别两个形状争用同一绕行空间。 |
| P5 `临时支点` | D5 | 5 | 玩家是否把真实 Block 当作另一个动作的临时支点。 |
| P6 `远处先完成` | D5 | 5 | 玩家是否从推侧约束推出远先近后的次序。 |
| P7 `把地图转过来` | D6 | 5 | 更换方向与形状后，P6 技巧是否真正迁移。 |
| P8 `三道门` | D6 | 5 | 玩家是否读出三个形状之间的依赖 DAG，而非穷举。 |
| P9 `差一格的近路` | D7 | 5 | 静态几何是否足以否定诱人的错误子目标。 |
| P10 `依次打开` | D8 | 8 | 顺序器洞察是否主导难度，且顿悟后执行保持短小。 |

P1–P6 的曲线与迁移未成立前，不进入 P7–P10 的正式评级；P7–P10 未通过前不制作 P11–P12 定稿。P12 还需作者验收与专家盲测，之后才允许横向生产其余 48 个新增关。

## Spike 概念反转专项

- 先按完整课程顺序测试 H1–H8；它们必须分散在非 Spike 技巧中，Spike 只作为辅助障碍，不得在最优解中触发 `object-reset` 或 `death-reset`。
- H8 结束后，先让测试者用自己的话解释 Spike。至少 4/5 玩家应主动避刺，并描述为“销毁／损失／回出生点恢复”，而不是计划中的远程移动工具。
- `mastery-v2` 第 70 槽至少进行 5 次盲测；至少 3/5 测试者应在无提示下重新解释同一规则，并用 origin 取得反向推侧。
- 第 71 槽要求测试者在操作前预测：玩家或物件占住完整 origin footprint 时，物件不会重生；揭示后至少 4/5 玩家能预测 origin 占用、玩家留在触发侧等边界。
- 记录测试者是否从静态出生点残影、接触爆炸和原位生成共同读出因果；若只能靠动画时序猜答案，关卡仍不合格。
- 在 H1–H8 尚未实现与完成顺序盲测前，实验室的三个 Spike 原型只能验证规则和表现，不能证明长期误导已经成立。
