# Oshi 63 关机制课程

> 历史基线文档：本页记录形成 60 个已验收基础关与 3 个 Spike 实验原型的上一版课程。当前生产默认只包含冻结的 60 关；后续正式路线以 [120 关大师课程实施地图](wayfinder/mastery-curriculum/map.md) 与 `src/course/mastery-blueprint.ts` 为准。

状态：**63 个候选盘面、课程导航与自动求解审计已实现；真人盲测待执行。**

作者验收状态（2026-09-03）：

- 插入新前导组之前的 60 关已由 Oshi 作者完整游玩并验收；顺延后对应当前 01–18 与 22–63。
- 新增的 19–21「Spike：销毁与重生」尚未做作者验收，保留为待验收候选关。
- 作者验收与陌生玩家盲测是两道不同门槛；前者通过不替代后者。

课程结构裁定（2026-09-03）：当前 19–21 只保留为机制与演出测试盘面，不再代表未来正式课程中的出现位置。正式路线不会在首次出现 Spike 后立刻安排三关“反转”；Spike 要先跨越一段课程时期，分散作为其它关卡族中的辅助障碍，待避刺认知经过间隔巩固后，才进入专门关卡族揭示 origin 回位的远程移动价值。

基础关保护裁定（2026-09-03）：作者已经验收的盘面与内部命题视为冻结资产。Spike 铺垫不得通过向旧盘面追加机关来改变原有推理、最短解、关键事件或紧凑性；若某个认知铺垫需要出现在两组既有课程之间，就新增独立的桥接关／插入关。稳定关卡 ID 与玩家看到的课程序号必须分离，使插关不导致旧关重编号、链接变化或存档映射失效。

设计依据：[Witness / SSR 证据研究](../research/15-witness-ssr-level-design-study.md)、[机制取舍调研](../research/12-mechanic-level-design-opportunities.md)、[可推性审计](../research/13-pushability-design-audit.md)。

## 1. 课程单位

每个关卡组围绕一个内部命题，固定包含三种角色：

| 角色 | 玩家经历 | 机器验收 |
| --- | --- | --- |
| 建立关 `establish` | 通过一个最小但完整的谜题形成规则假设。 | 有解；关键事件不可绕过；关键事件后最多 3 次有效交互。 |
| 定界关 `boundary` | 只改变一个因果变量，修正最可能的过度概括。 | `contrastVariable` 明确；对应反事实测试通过。 |
| 推演关 `inference` | 面对表面不可能的布局，推出稳定规则的二阶后果。 | 有解；命题谓词不可绕过；关键事件后最多 6 次有效推动。 |

内部的 `LevelTheorem` 不显示给玩家。界面只显示胜利目标、操作、组别、阶段和进度；规则由盘面表达。

## 2. 21 个三关组

| 关卡 | 组 | 建立 | 定界 | 推演 |
| --- | --- | --- | --- | --- |
| 01–03 | 推侧访问 | 从开放推侧完成单箱 | 目标近但缺站位仍不可推 | 完成顺序改变剩余推侧访问权 |
| 04–06 | 完整 footprint | 多格物整体移动 | 任一组成格受阻则整体不动 | 腾出的远端格成为通路或支点 |
| 07–09 | 完整覆盖 | 全部组成格都须覆盖 | 局部对齐不算完成 | 目标形状与最终推侧共同约束 |
| 10–12 | 数字匹配 | 同号 Goal | 错号与通配 Goal 对照 | 形状与身份约束同时满足 |
| 13–15 | 目标分配 | 两物体分配兼容目标 | 最近目标不一定正确 | 为选择唯一者保留稀缺目标 |
| 16–18 | Fake Block | Fake 可推但非任务 | 占目标仍不完成 | 把 Fake 当墙、停止器或支点 |
| 19–21 | Spike：销毁与重生 | Fake 触刺清路并在出生点重生 | 同一几何只改任务身份，真实 Block 也重生 | 先保留既有完成进度，再只重生触刺物件 |
| 22–24 | Spike：另一边 | 重生后玩家留在触发侧 | origin footprint 占用会阻止重生 | 管理占用来控制远程返回时机 |
| 25–27 | Spike：往返 | 回位后玩家留在触发侧 | 错时机会重新封路 | 把回位当成取得推侧的远程边 |
| 28–30 | 可移动 Goal | 移动 Goal 再完成 | Goal 同时改变目标与通路 | 规划终点落点和局部拓扑 |
| 31–33 | Goal 推 / 穿 | 前方有空间时被推 | 前方受阻时可穿过 | 主动切换两种状态取得另一侧 |
| 34–36 | Rain 停止点 | 滑到阻挡前 | 停止物位置改变落点 | 先建造刹车点再完成目标 |
| 37–39 | Rain 邻接动作 | 邻接可推物时只推一步 | 移开后同一输入恢复长滑 | 主动切换局部动作与滑行 |
| 40–42 | Gate 方向保持 | 沿入射方向出门 | 改出口结构但方向不重算 | 携带方向取得远端推侧 |
| 43–45 | Gate 远端条件 | 出口畅通时传送 | 出口受阻才尝试推入口 | 制造 / 解除阻挡来切换门的模式 |
| 46–48 | Gate 拓扑 | 推动端点 | 移动后配对不解除 | 使用新拓扑完成返回或最终推动 |
| 49–51 | Footprint × Spike | 任一组成格触发整体 | 回位检查完整 origin footprint | 远端组成格成为遥控触发器 |
| 52–54 | Goal × Rain | Goal 是雨中停止物 | 位置同时改变目标与停点 | 先当刹车、后当终点 |
| 55–57 | Rain × Gate | 滑行进入并穿过 Gate | 出口受阻不按普通传送处理 | 把滑行输入转化为推门序列 |
| 58–60 | Number × Fake | 同号 Fake 仍非任务 | 兼容性与任务身份分离 | 用 Fake 控制稀缺目标访问 |
| 61–63 | Gate × Spike | 端点触刺回 origin | 回位不解除配对 | 移动、使用、重置形成动态网络 |

Path Spike 暂不进入主课程。它若重新进入，必须先有静态路径预览、单步结算可读性和独立的时序课程。

## 3. 解锁结构

```text
01 建立 → 02 定界 ─┬→ 03 推演（可稍后完成）
                    ├→ Shape: 04…15
                    ├→ Fake: 16…18
                    ├→ Spike: 19…27
                    ├→ Goal: 28…33
                    ├→ Rain: 34…39
                    └→ Gate: 40…48

已掌握的两个前置分支 → 49…63 的对应组合组
```

- 完成一个组的定界关即获得该组的理解标记。
- 组内严格按建立 → 定界 → 推演开放。
- “下一关”优先进入本组三关；第三关后选择最低编号、已解锁且未完成的关。
- 22–24 只有在 19–21 全部完成后才开放；这项完整前置不会阻止玩家游玩其它已开放机制分支。
- 高难推演关不阻断其它已开放分支，但全部 63 关完成才计完整通关。
- 关卡图可随时返回已解锁关；胜利后只显示“下一关”，不自动跳转。

组合组前置：

- 49–51：完整 footprint + Spike 原位
- 52–54：可移动 Goal + Rain 停止点
- 55–57：Rain 停止点 + Gate 远端条件
- 58–60：数字匹配 + Fake Block
- 61–63：Spike 换侧 + Gate 拓扑

## 4. 数据与分层

```ts
type LessonRole = 'establish' | 'boundary' | 'inference';

interface LevelTheorem {
  axioms: string[];
  proposition: string;
  requiredPredicates: string[];
  criticalEvent: string;
  contrastVariable?: string;
  readabilityElements?: string[];
}

interface LevelSpec {
  id: string;
  groupId: string;
  role: LessonRole;
  prerequisites: string[];
  board: BoardDefinition;
  theorem: LevelTheorem;
}

interface CourseGroupDefinition {
  prerequisites: string[];
  completionPrerequisites?: string[];
}

interface LevelAnalysis {
  solvable: boolean;
  optimalMoves: number;
  optimalPushes: number;
  insightTailPushes: number;
  bypassExists: boolean;
  proofCriticalElements: string[];
}
```

- `game-engine.ts` 只处理确定性规则和领域事件。
- `authored-course.ts` 只保存盘面和必须发生的可观察谓词。
- `course-catalog.ts` 只保存课程、公理、命题与依赖。
- `level-analyzer.ts` 通过公开引擎接口求解，不能复制另一套规则。
- React 表现层只消费状态和类型化事件；Gate 动画、补间、图标不参与胜负判定。
- Spike death reset 保持独立路径，不复用手动 Restart。
- `object-reset` 事件携带实际 `contactCells`；棋盘只据此播放进入 Spike、接触爆炸和 origin 重生三段演出，不生成 Spike 到 origin 的空间轨迹。
- Spike 关以低对比度图标层显示出生 footprint，不改变格子颜色；reset 演出锁定方向输入 820ms，但 Undo / Restart 可立即取消。

## 5. TDD 关卡门槛

提交候选关的顺序固定为：

1. 从作者裁定和 Unity 源码添加机制对照测试。
2. 先写 `LevelTheorem` 和失败的命题测试。
3. 编写最小盘面直到有解。
4. 禁止每个 `requiredPredicate` 再求解；仍有解即存在命题绕过。
5. 回放求解器路线，记录最短移动、推动、关键事件和顿悟尾巴。
6. 对证明关键实体和格子做删除 / 封墙变异；区分证明元素与表现边框。
7. 通过 [盲测协议](playtest-protocol.md) 后才能从候选池晋升为正式关。

测试分工：

- 求解器证明“可解”和“不能绕过指定可观察后果”。
- 变异测试证明地图元素不是无关填充。
- UI 测试证明内部命题和提示没有泄漏给玩家。
- 真人盲测证明玩家能从静态盘面与稳定反馈理解命题。

当前自动化门槛已经运行在 63 个候选盘面上；真人盲测仍为空，因此不能把当前列表描述成最终定稿。

## 6. 下一阶段：难度与技巧掌握

作者完整游玩旧 60 关后的判断是：当前谜题总体有趣、曲线平缓，但绝大多数约为 1–3 / 10，机制后果与技巧迁移尚未充分展开。下一阶段不以继续横向增加新机制为优先，而要纵向深挖已经稳定的规则。

- 正式课程需要覆盖 1–10 / 10 的完整难度谱，而不是只把现有关卡整体拉长。
- 一个重要技巧不能只经历一次三关组；它还要经过变式练习、反向验证、跨布局迁移、与其它已学技巧组合，以及高难综合应用。
- 高难关应要求玩家识别并组合多个已学技巧，但每个因果步骤仍须静态可读、规则稳定、可撤销验证。
- 既有三关组继续承担“建立—定界—首次推演”；后续可以为同一技巧建立进阶组与组合组，形成从熟悉到举一反三的纵向阶梯。
- 难度 8–10 的题目必须来自二阶／三阶后果、顺序依赖、推侧访问、拓扑或资源冲突，不以冗长搬运、读图歧义或试错成本冒充深度。
- 旧 60 关作为已验收的基础课程保留；新增高难内容应通过新的命题测试、求解／变异审计及独立验收进入课程。

本阶段不再假定“三关组”足以让玩家形成稳定认知。每个值得深挖的技巧要经过一个可观察的信念生命周期：

1. **种下（seed）**：玩家通过一个低成本正例形成有用但尚不完整的直觉。
2. **巩固（reinforce）**：在不同布局、不同任务中重复使用，使它成为玩家会主动调用的策略。
3. **施压（stress）**：让常规应用接近失效，但仍不改变规则，以暴露直觉的边界。
4. **打破（overturn）**：设计一个按旧解释表面无解、按完整规则自然可解的关卡。
5. **迁移（transfer）**：换掉表面布局或任务身份，验证玩家掌握的是因果关系而非答案模板。
6. **综合（synthesize）**：与另一项已掌握技巧形成必要的顺序、推侧、拓扑或资源冲突。

“误导”只能来自玩家对真实经验的合理过度概括，不能来自虚假反馈、隐藏例外或文案欺骗。Spike 的销毁粒子和 origin 重生演出负责强化“销毁／出生点”解释；真正的反转关则证明同一条回位规则还能充当远程移动边。当前 19–21 只是一组机械前导候选，尚不足以独自建立长期的避刺直觉，也仍未通过作者验收。

Spike 的正式认知弧采用跨时期结构，而不是一个孤立三关组：

1. **首次威胁**：在非 Spike 题材关里把静态 Spike 放在显眼的错误路线；正解不触刺。
2. **分散巩固**：在至少三个其它机制族中改变 Spike 的障碍职责，例如封锁推侧、惩罚诱人捷径、限制完整 footprint、截断 Rain 停点。标题和关卡文案不强调 Spike。
3. **孵化间隔**：连续经历若干不以 Spike 为中心的关卡，让“应避开它”从短期记忆变成玩家默认策略。
4. **专门揭示**：后期 Spike 关构造按避刺模型表面无解的局面；首次要求 `object-reset` 成为不可绕过的必要事件，从而揭示回 origin 是远程移动边。
5. **迁移与综合**：再进入 origin 占用、换侧、footprint 遥控触发、Gate 拓扑等技巧升级。

辅助障碍关与专门揭示关采用相反的自动化契约：

- 辅助障碍关的预期解必须零触刺；禁止 `object-reset` 后仍有解，而删除 Spike 会产生绕过命题的捷径或显著降低约束。
- 专门揭示关必须发生指定 `object-reset`；禁止该事件后无解，证明玩家不能沿用“永远避刺”完成。
- 是否已经铺垫充分不由关卡数量单独决定；揭示前盲测玩家应主动避刺并将其解释为销毁／回出生点，否则继续增加或调整间隔巩固关。
- 上述辅助障碍原则只用于新增桥接关；不得据此改造已经验收的基础盘面。每个桥接关仍须围绕所在时期已学机制提出自己的命题，Spike 只承担约束，不抢走原技巧的归因。

完整的下一阶段路线、待决问题和依赖关系见 [难度 1–10 技巧掌握路线图](wayfinder/mastery-curriculum/map.md)。
