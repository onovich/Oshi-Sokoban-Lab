# 验证记录 — 2026-09-11

- Blender 5.2.1 LTS 无头生成成功，保留环境和人物 `.blend`，导出环境、箱子、人物 GLB。
- 人物 GLB 含 Idle / Walk / Push 三个骨骼动画片段，完成单位及不同参考姿势的转换。浏览器近景检查人物可见、正常尺寸，能显示推行双臂姿势。
- 实际“演示推行”：角色从 `(0, 4)` 移到约 `(0, -3.01)`；箱 1 从 `(0, 1.8)` 移到约 `(0, -4.13)`，随后恢复待机。
- 键盘输入后坐标从 `(0, 4)` 变为约 `(-0.02, 3.98)`；浏览器自动化为短按，持续按键留给人工试玩。
- 正交近景、全景 ISO、透视跟随已进行浏览器视觉检查；观察面板可实时切换投影和动作。
- 地面范围 Confiner：小边界显示无法收纳提示；X/Z 半尺寸调至 20 后显示可见地面已收纳。
- 实验碰撞/相机数学检查：6/6 通过，覆盖箱子推动、墙体阻挡、箱间阻挡、场地边界和视野约束。
- 主项目 `npm run typecheck`、`npm run build` 通过；实验 `npm run 3d:build` 通过。
- 浏览器显示约 60 FPS（本机当前视窗下的观察值，不是性能基准）。修复 Three.js 阴影常量弃用警告；没有观察到模型加载或渲染错误。
- `preview.png` 是 Blender 离线参考渲染；实际交互画面的光照以浏览器为准。

未进行正式关卡接入或正式游戏玩法验收。推箱动作仍是实验初版，保留原模型与战斗步法的风格差异。
